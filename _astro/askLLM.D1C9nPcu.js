import{t as e}from"./devBus.DRONl0Hn.js";import{a as t,i as n,n as r,o as i,t as a}from"./constants.th95jnuK.js";import{n as o}from"./engine.Bz12rB1G.js";import{t as s}from"./types.DZzuE3ID.js";var c=[`top_artists`,`top_tracks`,`top_albums`,`streams_per_month`,`streams_per_hour`,`streams_per_day_of_week`,`calendar_heatmap`,`session_analysis`,`artist_discovery`,`listening_rhythm`,`skip_rate`,`regularity`,`new_vs_old`,`favorite_weekday`,`total_streams`,`concentration_score`,`evolution_over_time`,`principal_platform`,`repeat_behavior`,`top_streak`,`seasonal_patterns`,`custom`],l={top_artists:{description:`Top artists ranked by stream count`,acceptsYear:!0,acceptsLimit:!0},top_tracks:{description:`Top tracks ranked by stream count`,acceptsYear:!0,acceptsLimit:!0},top_albums:{description:`Top albums ranked by stream count`,acceptsYear:!0,acceptsLimit:!0},streams_per_month:{description:`Streams aggregated per month within a year`,acceptsYear:!0,acceptsLimit:!1},streams_per_hour:{description:`Hourly stream distribution shown as a radial chart`,acceptsYear:!0,acceptsLimit:!1},streams_per_day_of_week:{description:`Streams aggregated per day of week`,acceptsYear:!0,acceptsLimit:!1},calendar_heatmap:{description:`Calendar heatmap of listening activity for a single year (year is required)`,acceptsYear:!0,acceptsLimit:!1},session_analysis:{description:`Listening session statistics (length, frequency)`,acceptsYear:!0,acceptsLimit:!1},artist_discovery:{description:`New vs cumulative artists discovered over time`,acceptsYear:!1,acceptsLimit:!1},listening_rhythm:{description:`Distribution of listening across morning, afternoon, evening, night`,acceptsYear:!0,acceptsLimit:!1},skip_rate:{description:`Share of streams that were skipped vs completed`,acceptsYear:!0,acceptsLimit:!1},regularity:{description:`How regularly the user listens (days with streams, longest pause)`,acceptsYear:!0,acceptsLimit:!1},new_vs_old:{description:`Streams of newly released music vs older catalog`,acceptsYear:!0,acceptsLimit:!1},favorite_weekday:{description:`Top weekday for listening`,acceptsYear:!0,acceptsLimit:!1},total_streams:{description:`Lifetime total of streams`,acceptsYear:!1,acceptsLimit:!1},concentration_score:{description:`How concentrated listening is across the top 5, 10, and 20 artists (share of total streams)`,acceptsYear:!0,acceptsLimit:!1},evolution_over_time:{description:`How total stream count has evolved year by year`,acceptsYear:!1,acceptsLimit:!1},principal_platform:{description:`Which platform or device (Android, iOS, Web, etc.) is used most for listening`,acceptsYear:!0,acceptsLimit:!1},repeat_behavior:{description:`Repeat listening statistics — consecutive replays of the same track`,acceptsYear:!0,acceptsLimit:!1},top_streak:{description:`Longest and most recent consecutive listening day streaks`,acceptsYear:!0,acceptsLimit:!1},seasonal_patterns:{description:`Distribution of listening across seasons (winter, spring, summer, fall)`,acceptsYear:!0,acceptsLimit:!1},custom:{description:`Fallback for any question not covered above. Requires a SELECT-only SQL string in the answer.`,acceptsYear:!1,acceptsLimit:!1}};function u(e){return typeof e==`string`&&c.includes(e)}var d=new Date().getFullYear(),f=new Date().toISOString().split(`T`)[0],p=`\
DuckDB schema (the user's music streaming history, fully local):

Table ${i} (one row per playback):
  track_uri TEXT, track_name TEXT, artist_name TEXT, album_name TEXT,
  ts TIMESTAMP (ISO 8601), ms_played INTEGER (milliseconds), platform TEXT

Table ${r}:
  stream_date DATE, stream_count DOUBLE, ms_played DOUBLE

Table ${a}:
  artist_name TEXT, first_year INTEGER

Table ${n}:
  session_id INTEGER, track_count DOUBLE, duration_ms DOUBLE,
  session_start TIMESTAMP, session_end TIMESTAMP

Table ${t}: aggregate stats cache (rarely needed).

Notes:
- Use ts::date for grouping by day. EXTRACT(year FROM ts) for year.
- ms_played is milliseconds (divide by 60000 for minutes).
- Only SELECT or WITH..SELECT statements are allowed in custom SQL.
`;function m(){return Object.keys(l).map(e=>{let t=l[e],n=[];return t.acceptsYear&&n.push(`year (int, optional)`),t.acceptsLimit&&n.push(`limit (int, optional)`),`  - ${e}${n.length?` [params: ${n.join(`, `)}]`:``}: ${t.description}`}).join(`
`)}var h=`\
You are a routing assistant for a local music-listening data app.

Given a user question, you choose ONE intent from the catalog below, fill in optional params, and return a single JSON object — nothing else.

${p}

Available intents (pick exactly one):
${m()}

Output format (return EXACTLY one JSON object, no prose, no code fences, no comments, double-quoted keys):
{
  "intent": "<one of the intent names above>",
  "params": { "year": <int|omit>, "limit": <int|omit> },
  "title": "<short chart title>",
  "explanation": "<one sentence: what the chart shows>",
  "sql": "<SELECT or WITH..SELECT representing this question, no semicolon, no DDL/DML>"
}

Rules:
- "intent" MUST be one of the catalog names.
- "sql" is ALWAYS required for every intent, including non-custom ones.
- "params.year" only if user mentioned a year. Omit otherwise.
- "params.limit" only for top_artists/top_tracks/top_albums when user asked for a specific N.
- Use intent "custom" ONLY if no other intent fits.
- Never invent table or column names. Never include any text outside the JSON object.
- Today is ${f}. If the message prefix contains "The user is asking about year X", use X in the SQL and params. Otherwise omit year from params.
`,g=[{user:`Who are my top artists?`,assistant:JSON.stringify({intent:`top_artists`,params:{},title:`Top artists`,explanation:`Artists ranked by total stream count.`,sql:`SELECT artist_name, COUNT(*)::DOUBLE AS count_streams, SUM(ms_played)::DOUBLE AS ms_played FROM ${i} WHERE artist_name IS NOT NULL GROUP BY artist_name ORDER BY count_streams DESC LIMIT 5`})},{user:`[Today is ${f}. The user is asking about year ${d}.] What are my top artists this year?`,assistant:JSON.stringify({intent:`top_artists`,params:{year:d},title:`Top artists ${d}`,explanation:`Artists ranked by total stream count in ${d}.`,sql:`SELECT artist_name, COUNT(*)::DOUBLE AS count_streams, SUM(ms_played)::DOUBLE AS ms_played FROM ${i} WHERE artist_name IS NOT NULL AND EXTRACT(year FROM ts) = ${d} GROUP BY artist_name ORDER BY count_streams DESC LIMIT 5`})},{user:`[Today is ${f}. The user is asking about year ${d-1}.] What are my top tracks from last year?`,assistant:JSON.stringify({intent:`top_tracks`,params:{year:d-1},title:`Top tracks ${d-1}`,explanation:`Most-played tracks during ${d-1}.`,sql:`SELECT track_name, artist_name, COUNT(*)::DOUBLE AS count_streams, SUM(ms_played)::DOUBLE AS ms_played FROM ${i} WHERE EXTRACT(year FROM ts) = ${d-1} AND track_name IS NOT NULL GROUP BY track_name, artist_name ORDER BY count_streams DESC LIMIT 5`})},{user:`Show me my top 10 tracks in 2023`,assistant:JSON.stringify({intent:`top_tracks`,params:{year:2023,limit:10},title:`Top 10 tracks in 2023`,explanation:`Most-played tracks during 2023.`,sql:`SELECT track_name, artist_name, COUNT(*)::DOUBLE AS count_streams, SUM(ms_played)::DOUBLE AS ms_played FROM ${i} WHERE EXTRACT(year FROM ts) = 2023 AND track_name IS NOT NULL GROUP BY track_name, artist_name ORDER BY count_streams DESC LIMIT 10`})},{user:`[Today is ${f}. The user is asking about year ${d}.] How does my listening look across the year?`,assistant:JSON.stringify({intent:`calendar_heatmap`,params:{year:d},title:`Listening calendar`,explanation:`Daily listening intensity across the calendar year.`,sql:`SELECT ts::date AS stream_date, COUNT(*)::DOUBLE AS stream_count FROM ${i} WHERE EXTRACT(year FROM ts) = ${d} GROUP BY ts::date ORDER BY stream_date`})},{user:`Streams per month for 2022`,assistant:JSON.stringify({intent:`streams_per_month`,params:{year:2022},title:`Streams per month — 2022`,explanation:`Monthly stream counts for the year 2022.`,sql:`SELECT DATE_TRUNC('month', ts) AS month, COUNT(*)::DOUBLE AS count_streams, SUM(ms_played)::DOUBLE AS ms_played FROM ${i} WHERE EXTRACT(year FROM ts) = 2022 GROUP BY month ORDER BY month`})},{user:`How many minutes per platform did I listen on?`,assistant:JSON.stringify({intent:`custom`,params:{},title:`Minutes listened per platform`,explanation:`Total minutes of playback grouped by reported platform.`,sql:`SELECT platform, SUM(ms_played) / 60000.0 AS minutes FROM ${i} WHERE platform IS NOT NULL GROUP BY platform ORDER BY minutes DESC`})},{user:`What is my skip rate?`,assistant:JSON.stringify({intent:`skip_rate`,params:{},title:`Skip rate`,explanation:`Share of streams that were skipped vs completed.`,sql:`SELECT COUNT(*) FILTER (WHERE ms_played < 30000)::DOUBLE AS skipped_listens, COUNT(*) FILTER (WHERE ms_played >= 30000)::DOUBLE AS complete_listens FROM ${i}`})},{user:`How has my listening grown over the years?`,assistant:JSON.stringify({intent:`evolution_over_time`,params:{},title:`Listening evolution`,explanation:`Total stream count per year across all years.`,sql:`SELECT year(ts::date)::integer AS stream_year, COUNT(*)::DOUBLE AS stream_count, SUM(ms_played)::DOUBLE AS ms_played FROM ${i} GROUP BY year(ts::date) ORDER BY year(ts::date)`})},{user:`What's my longest listening streak?`,assistant:JSON.stringify({intent:`top_streak`,params:{},title:`Listening streaks`,explanation:`Longest and most recent consecutive listening day streaks.`,sql:`SELECT COUNT(*)::integer AS streaks, MIN(ts::date) AS start_ts, MAX(ts::date) AS end_ts FROM ${i} GROUP BY ts::date ORDER BY streaks DESC LIMIT 1`})},{user:`What app or device do I use most to listen?`,assistant:JSON.stringify({intent:`principal_platform`,params:{},title:`Principal platform`,explanation:`Distribution of listening across platforms and devices.`,sql:`SELECT platform, COUNT(*)::DOUBLE AS stream_count FROM ${i} WHERE platform IS NOT NULL GROUP BY platform ORDER BY stream_count DESC LIMIT 5`})},{user:`My top albums of all time`,assistant:JSON.stringify({intent:`top_albums`,params:{},title:`Top albums`,explanation:`Albums ranked by total stream count.`,sql:`SELECT album_name, artist_name, COUNT(*)::DOUBLE AS count_streams, SUM(ms_played)::DOUBLE AS ms_played FROM ${i} WHERE album_name IS NOT NULL GROUP BY album_name, artist_name ORDER BY count_streams DESC LIMIT 5`})},{user:`What hours of the day do I listen most?`,assistant:JSON.stringify({intent:`streams_per_hour`,params:{},title:`Streams by hour`,explanation:`Number of streams for each hour of the day.`,sql:`SELECT EXTRACT(hour FROM ts)::INTEGER AS play_hour, COUNT(*)::DOUBLE AS count_streams, SUM(ms_played)::DOUBLE AS ms_played FROM ${i} GROUP BY play_hour ORDER BY play_hour`})},{user:`Am I a morning or a night listener?`,assistant:JSON.stringify({intent:`listening_rhythm`,params:{},title:`Listening rhythm`,explanation:`Share of listening across morning, afternoon, evening and night.`,sql:`SELECT COUNT(*) FILTER (WHERE EXTRACT(hour FROM ts) BETWEEN 6 AND 11)::DOUBLE AS morning, COUNT(*) FILTER (WHERE EXTRACT(hour FROM ts) BETWEEN 12 AND 17)::DOUBLE AS afternoon, COUNT(*) FILTER (WHERE EXTRACT(hour FROM ts) BETWEEN 18 AND 23)::DOUBLE AS evening, COUNT(*) FILTER (WHERE EXTRACT(hour FROM ts) BETWEEN 0 AND 5)::DOUBLE AS night, COUNT(*)::DOUBLE AS total FROM ${i}`})},{user:`How does my listening change by season?`,assistant:JSON.stringify({intent:`seasonal_patterns`,params:{},title:`Seasonal patterns`,explanation:`Share of listening across winter, spring, summer and fall.`,sql:`SELECT COUNT(*) FILTER (WHERE EXTRACT(month FROM ts) IN (12, 1, 2))::DOUBLE AS winter, COUNT(*) FILTER (WHERE EXTRACT(month FROM ts) IN (3, 4, 5))::DOUBLE AS spring, COUNT(*) FILTER (WHERE EXTRACT(month FROM ts) IN (6, 7, 8))::DOUBLE AS summer, COUNT(*) FILTER (WHERE EXTRACT(month FROM ts) IN (9, 10, 11))::DOUBLE AS fall, COUNT(*)::DOUBLE AS total FROM ${i}`})},{user:`Which weekday do I listen the most?`,assistant:JSON.stringify({intent:`favorite_weekday`,params:{},title:`Favorite weekday`,explanation:`Streams per weekday with the share each day represents.`,sql:`SELECT dayname(ts) AS day_name, COUNT(*)::DOUBLE AS stream_count, SUM(ms_played)::DOUBLE AS ms_played, (COUNT(*) * 100.0 / SUM(COUNT(*)) OVER ())::DOUBLE AS pct FROM ${i} GROUP BY day_name ORDER BY stream_count DESC`})}];function _(e){let t=new Date().getFullYear();if(/\blast\s+year\b/i.test(e))return t-1;if(/\b(this|current)\s+year\b/i.test(e))return t;let n=e.match(/\b(20\d{2})\b/);if(n)return parseInt(n[1],10)}function v(e,t){let n=[{role:`system`,content:h}];for(let e of g)n.push({role:`user`,content:e.user}),n.push({role:`assistant`,content:e.assistant});let r=t.slice(-8);for(let e of r)n.push({role:e.role,content:e.text});let i=_(e),a=i?` The user is asking about year ${i}.`:``;return n.push({role:`user`,content:`[Today is ${f}.${a}] ${e}`}),n}function y(e){let t=e.match(/```(?:json)?\s*([\s\S]*?)\s*```/i),n=t?t[1]:e,r=n.indexOf(`{`);if(r===-1)throw new s(`No JSON object found in model output.`,`parse`);let i=0,a=!1,o=!1;for(let e=r;e<n.length;e++){let t=n[e];if(o){o=!1;continue}if(t===`\\`){o=!0;continue}if(t===`"`){a=!a;continue}if(!a){if(t===`{`)i++;else if(t===`}`&&(i--,i===0))return n.slice(r,e+1)}}throw new s(`Unbalanced JSON braces in model output.`,`parse`)}function b(e){let t=y(e),n;try{n=JSON.parse(t)}catch{throw new s(`Model output is not valid JSON.`,`parse`)}if(typeof n!=`object`||!n)throw new s(`Model output is not a JSON object.`,`schema`);let r=n;if(!u(r.intent))throw new s(`Unknown or missing intent: ${String(r.intent)}`,`schema`);let i=r.intent,a=r.params??{},o={};typeof a.year==`number`&&Number.isFinite(a.year)&&(o.year=Math.trunc(a.year)),typeof a.limit==`number`&&Number.isFinite(a.limit)&&(o.limit=Math.trunc(a.limit));let c=typeof r.title==`string`&&r.title.trim().length>0?r.title.trim():`Result`,l=typeof r.explanation==`string`?r.explanation.trim():``,d=typeof r.sql==`string`?r.sql.trim():``;if(d.length===0)throw new s(`Response is missing the required sql field.`,`schema`);return{intent:i,params:o,title:c,explanation:l,sql:d}}async function x(t,n,r,i){let a=v(n,r),c,l=performance.now();try{let n=t.chat.completions.create({messages:a,temperature:.1,max_tokens:512});if(i){let e=new Promise((e,t)=>{if(i.aborted){t(new s(`Cancelled`,`aborted`));return}i.addEventListener(`abort`,()=>t(new s(`Cancelled`,`aborted`)))});c=await Promise.race([n,e])}else c=await n;let r=performance.now()-l,u=c.usage?.completion_tokens??0;e.emit(`webllm:inference`,{model:o(),durationMs:r,tokensPerSec:r>0?u/(r/1e3):0})}catch(e){throw e instanceof s&&e.kind===`aborted`?e:new s(e instanceof Error?e.message:String(e),`engine`)}if(!(`choices`in c)||!c.choices?.length||!c.choices[0].message?.content)throw new s(`Empty response from model.`,`engine`);return b(c.choices[0].message.content)}export{x as askLLM,y as extractJsonObject,b as parseChatAnswer};